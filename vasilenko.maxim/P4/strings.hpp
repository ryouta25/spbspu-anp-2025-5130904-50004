#ifndef STRINGS_HPP
#define STRINGS_HPP

#include <cstddef>

namespace vasilenko
{
  char* findMissingLetters(const char* src, char* dest);
  char* mergeStrings(const char* src1, const char* src2, char* dest);
}

#endif
